#include <stdio.h>

/**
* main - Entry point
*
* Return: returns 0 when everything works fine
*
*/

int main(void)
{
	int n;

	for (n = 0; n < 10; ++n)
	{
		printf("%d", n);
	}
	putchar(10);
	return (0);
}
